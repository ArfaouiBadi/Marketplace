{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php bin/console doctrine:migrations:migrate --no-interaction && apache2-foreground",
        "restartPolicyType": "ON_FAILURE"
    }
}